# Public evidence

Only sanitized, schema-versioned exports from the private lineage laboratory belong here.

Each evidence object should be independently parseable and contain its own lineage/state/protocol identifiers plus cryptographic commitments.

Forbidden here: raw checkpoints, raw event archives, private corpora, source edge identities, private parameter manifests and private code.
